
#include "SpriteTest.h"


TEST_F(SpriteTest, createCorrectEmptyModel) {
//TODO:Potrzebne zmiany w silniku.
 // auto standardModel = sut_.getStandardModelForData();
 /// QStandardItemModel* positionModel = standardModel[0];
  //QStandardItemModel* programModel = standardModel[1];
 // QStandardItemModel* objPathModel = standardModel[2];
  //Potrzebujemy zwrócić nazwę obiektu
  //EXPECT_EQ(sut_.getObjectName(), "name");

 // EXPECT_EQ((positionModel->item(0,1))->text().toFloat(), 0);
 // EXPECT_EQ((positionModel->item(1,1))->text().toFloat(), 0);
 // EXPECT_EQ((positionModel->item(2,1))->text().toFloat(), 0);
  // Tu sprawdzamy jaki typ programu
  // np.
  // EProgram::basic. Potrzebne info o tym jaki mamy program czyli np. flw::flf::EProgram getProgram();
  //EXPECT_EQ((programModel->item(0,1))->text(), "basic");

 // EXPECT_EQ((objPathModel->item(0,1))->text(), "/home/radek/obj.obj");
}